create or replace table  "DATA_WORLD_METADATA"."PUBLIC"."DDW_DATASOURCE"
(
    org text,
    collections text,
    databaseiri text,
    databaseName text,
    jdbcURL text,
    databaseServer text,
    databasePort text,
    schemas text
);